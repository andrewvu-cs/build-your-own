import React from 'react'
import styles from './Logo.module.scss'

const Logo = () => {
    return (
        <div className={styles.Logo}>
            <p>Logo</p>
        </div>
    )
}

export default Logo
